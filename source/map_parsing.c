#include "../includes/cub3d.h"

static int	error(char* err_msg, int return_value)
{
	printf("%s\n", err_msg);
	return (exit_status)
}

int is_valid_map(t_data *data, char *map_filename)
{
	// reopen map, populate char **map and read for invalidities
	;
}