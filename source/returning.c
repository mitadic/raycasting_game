#include "../includes/cub3d.h"

int	error(char* err_msg, int return_value)
{
	printf("%s\n", err_msg);
	return (return_value);
}

int purge(t_data *data, int exit_status)
{
	/*free this, free that*/
    return(exit_status);
}